namespace Mosaic.Bridge.Tools.Packages
{
    public sealed class PackageListParams
    {
        /// <summary>When true, uses offline/cached mode instead of querying the registry. Defaults to false.</summary>
        public bool OfflineMode { get; set; }
    }
}
